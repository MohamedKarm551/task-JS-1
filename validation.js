export function isEmpty(str) {
    // let txtValidation = document.getElementById("validation").value;
    if (str == "") {
        alert("empty");
    } else {
        console.log(str);
        alert("empty length : " + str.length);
    }

}